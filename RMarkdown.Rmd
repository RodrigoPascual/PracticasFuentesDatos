---
title: "RMarkdown"
output:
  html_document: default
  word_document: default
date: "2022-10-24"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


# SEMINARIO FUENTES DE BASES DE DATOS
## Relacion del sedentarismo con enfermedades

## Introduccion
En nuestro seminario vamos a intentar relacionar dos tablas de sedentarismo y enfermedades

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

#introduccion
#objetivos: max 3 para cada obketivo un metodo y un resultado

#metodos: las paquetes usadas
#resultado: tabla y una grafica
#conclusion

## Paquetes usadas:
readr
ggplot2
dplyr
tidyverse
ggplot2
forcats
hrbrthemes


## Explicación de los resultados de las tablas:

data <- data.frame(enfermedades,sedentarismo)
En este siguiente parametro lo asignamos las dos tablas 





##Conclusión




```{r}
library(readr)
enfermedades <- read_delim("input/enfermedades_cronicas.csv", 
    delim = ";", escape_double = FALSE, trim_ws = TRUE)
```


```{r enfermedades}
summary(enfermedades)
plot(enfermedades)
```

## Including Plots

You can also embed plots, for example:

```{r}
library(readr)
sedentarismo <- read_delim("input/sedentarismo.csv", 
    delim = ";", escape_double = FALSE, trim_ws = TRUE)
```



```{r sedentarismo}
summary(sedentarismo)
plot(sedentarismo)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
